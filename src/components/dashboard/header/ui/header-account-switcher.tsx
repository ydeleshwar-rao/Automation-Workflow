"use client";

/**
 * HeaderAccountSwitcher — REMOVED
 * ─────────────────────────────────────────────────────────────
 * The client-switching concept has been removed. Every user
 * (admin or developer) operates on their own account only.
 *
 * This component now renders nothing. It is kept as a stub so
 * that any existing imports compile without errors while the
 * header is gradually cleaned up.
 */

export function HeaderAccountSwitcher() {
  return null;
}
