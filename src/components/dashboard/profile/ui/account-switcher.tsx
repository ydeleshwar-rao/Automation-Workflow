"use client";

/**
 * AccountSwitcher — REMOVED
 * ─────────────────────────────────────────────────────────────
 * The client-switching concept has been removed.
 * Every user operates on their own account only.
 * This stub is kept so existing imports compile without errors.
 */

export function AccountSwitcher() {
  return null;
}
