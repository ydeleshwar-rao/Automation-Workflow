"use client";

/**
 * AssignClientsModal — REMOVED
 * ─────────────────────────────────────────────────────────────
 * The client-assignment concept has been removed.
 * This file is kept as a stub so that any stale imports compile.
 *
 * Developer ↔ Client assignments no longer exist.
 * Use DeveloperPermissionsModal to manage per-page access instead.
 */

export function AssignClientsModal() {
  return null;
}
